fastapi>=0.115.0
uvicorn[standard]>=0.30.6
sqlalchemy>=2.0.35
pydantic>=2.10.1
httpx>=0.27.2
croniter>=3.0.3
cryptography>=43.0.1
PyJWT>=2.9.0
python-multipart>=0.0.9
websockets>=13.1
# Pinned to a major: fastmcp 4 brought in mcp 2.x, whose client API renamed
# streamablehttp_client and Tool.inputSchema — an unpinned bump silently broke
# every http MCP server's tool sync. Test before widening this range.
fastmcp>=4.0.0,<5

# PII-herkenning voor de data-guard (namen, plaatsen, telefoonnummers) —
# de helft die je met patronen niet krijgt. Optioneel in de code: ontbreekt het
# pakket of het taalmodel, dan werkt de guard door op zijn eigen detectors en
# zegt de statuspagina dat deze laag uit staat. Het Nederlandse spaCy-model
# wordt in de Dockerfile opgehaald (~13 MB); zonder dat model start Presidio
# niet.
presidio-analyzer>=2.2
